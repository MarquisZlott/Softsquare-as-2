import { RandomCounterDirective } from './random-counter.directive';

describe('RandomCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomCounterDirective();
    expect(directive).toBeTruthy();
  });
});
