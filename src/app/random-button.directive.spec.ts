import { RandomButtonDirective } from './random-button.directive';

describe('RandomButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomButtonDirective();
    expect(directive).toBeTruthy();
  });
});
